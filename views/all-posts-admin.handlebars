
{{#each posts as |post|}}
  <div class="post">
    <div class="post-header">
      <span>
        <a href="/dashboard/edit/{{post.id}}">
          <h2 class="text-left" id="post-title">{{post.title}}</h2>
        </a>
      </span>
      <span>
      <p class="text-right" id="post-info">{{format_date post.date_created}}</p>
      </span>
    </div>
    <div class = "post-body">
      {{post.body}}
    </div>
  </div>

{{/each}}


{{!-- <script src="/js/post.js"></script> --}}